# AppKu Component Smoke App

This Vite app exercises the public package entrypoint for `appku-common-components`.

Use it to verify that the library builds and runs with React 19, Ant Design, and AG Grid Community:

```bash
npm install
npm run build
```

Owned code in this app is ESM JavaScript and JSX. Do not add TypeScript or TSX files.
